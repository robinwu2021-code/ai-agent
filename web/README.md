# AI Agent Web

Next.js 15 frontend for AI Agent.

## Setup

```bash
cd web
npm install
npm run dev
```

Backend must be running on port 8000:
```bash
cd ..
python server.py --port 8000
```

Open http://localhost:3000
